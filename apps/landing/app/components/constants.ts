/** Canonical origin of the landing, used in meta tags and the sitemap. */
export const SITE_URL = 'https://e-bio.org'

/**
 * Single contact address for support, legal and privacy matters. The legal
 * pages and the stores' data-safety forms all point here, so the mailbox has
 * to exist before publication.
 */
export const CONTACT_EMAIL = 'contact@e-bio.org'
