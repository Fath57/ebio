# URL de l'API qui sert le contenu éditable du site (sections + FAQ).
# Sans cette variable, le site retombe sur ses textes et captures embarqués :
# rien de ce qui est publié depuis le back-office n'apparaît.
API_URL=http://localhost:3000
