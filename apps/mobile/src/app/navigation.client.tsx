// Client variant entry. Until T037 trims the supplier screens out, it simply
// re-exports the historical navigator so the published app keeps working as-is.
export { AppNavigation as ClientNavigation } from './navigation'
